# Assignment3 Frontend - Vue.js приложение

Современный фронтенд для благотворительной платформы Assignment3, построенный на Vue.js 3 с использованием Pinia для управления состоянием и Vue Router для навигации.

## 📋 Описание

Веб-интерфейс для платформы Assignment3, предоставляющий пользователям интуитивный и удобный способ взаимодействия с системой благотворительности.

## 🚀 Технологии

- **Framework**: Vue.js 3
- **State Management**: Pinia
- **Routing**: Vue Router 4
- **Build Tool**: Vite
- **HTTP Client**: Axios
- **Styling**: CSS3 (Custom)
- **Node Version**: ^20.19.0 || >=22.12.0

## 📦 Требования

- Node.js 20.19.0 или выше (или 22.12.0+)
- npm или yarn
- Git

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd project-vue
```

### 2. Установка зависимостей

```bash
npm install
```

или

```bash
yarn install
```

### 3. Настройка API

Убедитесь, что backend сервер запущен на `http://localhost:8080` или измените базовый URL в файле `src/api/index.js`.

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### 5. Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### 6. Предпросмотр продакшен сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
project-vue/
├── public/              # Статические файлы
├── src/
│   ├── api/            # API конфигурация
│   ├── components/     # Vue компоненты
│   │   ├── EventCard.vue
│   │   └── ProjectCard.vue
│   ├── router/         # Vue Router конфигурация
│   ├── stores/         # Pinia stores
│   │   ├── useAuthStore.js
│   │   ├── useProjectStore.js
│   │   ├── useUserStore.js
│   │   ├── useCategoryStore.js
│   │   ├── useEventStore.js
│   │   └── useDonationStore.js
│   ├── views/          # Страницы
│   │   ├── auth/       # Авторизация
│   │   ├── projects/   # Проекты
│   │   ├── events/     # События
│   │   ├── categories/ # Категории
│   │   ├── users/      # Пользователи
│   │   └── admin/      # Админ панель
│   ├── App.vue         # Главный компонент
│   └── main.js         # Точка входа
├── package.json
└── vite.config.js
```

## 🎨 Основные страницы

### Публичные страницы
- **Home** (`/`) - Главная страница с проектами и статистикой
- **Projects** (`/projects`) - Список всех проектов
- **Project Detail** (`/projects/:id`) - Детальная информация о проекте
- **Events** (`/events`) - Список волонтерских событий
- **Event Detail** (`/events/:id`) - Детальная информация о событии
- **Categories** (`/categories`) - Список категорий
- **About** (`/about`) - О нас
- **Contacts** (`/contacts`) - Контакты

### Страницы для авторизованных пользователей
- **Login** (`/login`) - Вход в систему
- **Register** (`/register`) - Регистрация
- **Profile** (`/profile`) - Профиль пользователя
- **Profile Edit** (`/profile/edit`) - Редактирование профиля
- **User Profile** (`/users/:id`) - Профиль другого пользователя
- **Donate** (`/projects/:id/donate`) - Страница пожертвования

### Страницы для NEEDS_HELP
- **Project Create** (`/projects/create`) - Создание проекта
- **Project Edit** (`/projects/:id/edit`) - Редактирование проекта

### Страницы для ADMIN
- **Verification** (`/verification`) - Верификация пользователей и проектов
- **Category Create** (`/categories/create`) - Создание категории
- **Category Edit** (`/categories/:id/edit`) - Редактирование категории
- **Event Create** (`/events/create`) - Создание события
- **Event Edit** (`/events/:id/edit`) - Редактирование события

## 🔐 Аутентификация

Приложение использует JWT токены для аутентификации. Токен хранится в cookies и автоматически добавляется к каждому запросу.

### Роли пользователей

- **DONOR** - Донор (может делать пожертвования)
- **NEEDS_HELP** - Нуждающийся (может создавать проекты после верификации)
- **ADMIN** - Администратор (полный доступ)

## 🎯 Основные функции

### Для всех пользователей
- Просмотр проектов и событий
- Просмотр категорий
- Просмотр статистики пожертвований
- Регистрация и вход

### Для доноров (DONOR)
- Создание пожертвований
- Просмотр истории пожертвований

### Для нуждающихся (NEEDS_HELP)
- Создание проектов (после верификации)
- Редактирование своих проектов
- Загрузка документов для верификации

### Для администраторов (ADMIN)
- Верификация пользователей и проектов
- Управление категориями (CRUD)
- Управление событиями (CRUD)
- Удаление проектов

## 🎨 UI/UX Особенности

- Адаптивный дизайн для всех устройств
- Современный и чистый интерфейс
- Плавные анимации и переходы
- Карусель изображений для проектов
- Карточки событий с красивым дизайном
- Интуитивная навигация

## 📱 Адаптивность

Приложение полностью адаптивно и корректно отображается на:
- Десктопах (1920px+)
- Планшетах (768px - 1024px)
- Мобильных устройствах (< 768px)

## 🔧 Конфигурация

### API Base URL

Измените базовый URL API в `src/api/index.js`:

```javascript
const api = axios.create({
  baseURL: 'http://localhost:8080/api/v1',
  // ...
})
```

## 📦 Сборка и деплой

### Production сборка

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

## 🐛 Известные проблемы

Если вы столкнулись с проблемами, проверьте:
1. Запущен ли backend сервер
2. Правильно ли настроен API URL
3. Установлены ли все зависимости

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 📞 Контакты

Для вопросов и предложений создайте Issue в репозитории.
